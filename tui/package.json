{
  "name": "helena-tui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TUI da Helena (OpenTUI/React, Bun). Fala SÓ com a API local do daemon (client/src/local-api) — nunca com o backend.",
  "scripts": {
    "start": "bun src/main.tsx",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun run scripts/build.ts"
  },
  "dependencies": {
    "@opentui/core": "0.5.11",
    "@opentui/qrcode": "0.5.11",
    "@opentui/react": "0.5.11",
    "react": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "bun": "^1.4.2",
    "bun-types": "^1.4.2",
    "typescript": "^5.9.0"
  }
}
